# Component Architecture Overview

## 📊 Visual Structure

```
┌─────────────────────────────────────────────────────────────────┐
│                      Root Layout (layout.tsx)                    │
│                                                                   │
│  ┌────────────────┐  ┌────────────────────────────────────────┐ │
│  │   Sidebar      │  │         Main Content Area              │ │
│  │  (Fixed Left)  │  │                                        │ │
│  │                │  │  ┌──────────────────────────────────┐  │ │
│  │ ┌────────────┐ │  │  │        Header (Sticky)           │  │ │
│  │ │  Logo +    │ │  │  │  [Search] [Notif] [Profile]      │  │ │
│  │ │  Brand     │ │  │  └──────────────────────────────────┘  │ │
│  │ └────────────┘ │  │                                        │ │
│  │                │  │  ┌──────────────────────────────────┐  │ │
│  │ Navigation:    │  │  │                                  │  │ │
│  │ 🏠 Dashboard   │  │  │      Page Content (children)     │  │ │
│  │ 👥 Users       │  │  │                                  │  │ │
│  │ 📄 Documents   │  │  │   - Dashboard Stats              │  │ │
│  │ 📊 Analytics   │  │  │   - Activity Feed                │  │ │
│  │ ⚙️ Settings    │  │  │   - Any Page Content             │  │ │
│  │                │  │  │                                  │  │ │
│  │ ┌────────────┐ │  │  └──────────────────────────────────┘  │ │
│  │ │  Profile   │ │  │                                        │ │
│  │ │  Section   │ │  │                                        │ │
│  │ └────────────┘ │  │                                        │ │
│  └────────────────┘  └────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────────┘
```

## 🗂️ Component Hierarchy

```
App
└── RootLayout (layout.tsx)
    ├── Sidebar (components/layout/Sidebar.tsx)
    │   ├── Logo/Brand Section
    │   ├── Navigation Menu
    │   │   ├── Dashboard Link (Home Icon)
    │   │   ├── Users Link (Users Icon)
    │   │   ├── Documents Link (FileText Icon)
    │   │   ├── Analytics Link (BarChart3 Icon)
    │   │   └── Settings Link (Settings Icon)
    │   └── Profile Section
    │
    └── Main Content Wrapper
        ├── Header (components/layout/Header.tsx)
        │   ├── Search Input (Search Icon)
        │   ├── Notifications Button (Bell Icon + Badge)
        │   └── Profile Button (User Icon)
        │
        └── Page Content (children)
            └── Dashboard (app/page.tsx)
                ├── Page Header
                ├── Stats Cards Grid
                │   ├── Total Users Card (Users Icon)
                │   ├── Revenue Card (DollarSign Icon)
                │   ├── Active Projects Card (FolderKanban Icon)
                │   └── Tasks Card (CheckCircle2 Icon)
                └── Recent Activity Section
                    ├── User Registration (UserPlus Icon)
                    ├── Project Completion (Package Icon)
                    ├── Payment (CreditCard Icon)
                    └── Task Assignment (FileEdit Icon)
```

## 🎯 Component Flow

```
┌──────────────────────────────────────────────────────────┐
│                    User Interaction                       │
└────────────┬─────────────────────────────────────────────┘
             │
             ▼
┌──────────────────────────────────────────────────────────┐
│              Navigation Click (Sidebar)                   │
│  • Click on Dashboard, Users, Documents, etc.             │
└────────────┬─────────────────────────────────────────────┘
             │
             ▼
┌──────────────────────────────────────────────────────────┐
│             Next.js Client-Side Routing                   │
│  • Route to new page (/users, /documents, etc.)           │
└────────────┬─────────────────────────────────────────────┘
             │
             ▼
┌──────────────────────────────────────────────────────────┐
│              Layout Persists (No Re-render)               │
│  • Sidebar stays fixed                                    │
│  • Header stays sticky                                    │
└────────────┬─────────────────────────────────────────────┘
             │
             ▼
┌──────────────────────────────────────────────────────────┐
│           Only {children} Content Changes                 │
│  • New page component renders in main area                │
└──────────────────────────────────────────────────────────┘
```

## 📦 Import/Export Pattern

```typescript
// Layout Components
import { Sidebar, Header } from "@/components/layout";

// UI Components  
import { 
  Loader, 
  FullPageLoader, 
  InlineLoader,
  Button,
  Card,
  CardHeader,
  CardBody,
  CardFooter,
  Input
} from "@/components/ui";

// Icons
import { 
  Home, 
  Users, 
  FileText, 
  BarChart3, 
  Settings,
  Search,
  Bell,
  User,
  Menu,
  X
} from "lucide-react";
```

## 🎨 State Management

```
┌─────────────────────────────────────────────────────────┐
│                    Sidebar Component                     │
│                                                           │
│  Local State:                                             │
│  • isOpen: boolean (controls mobile menu visibility)      │
│                                                           │
│  Interactions:                                            │
│  • Mobile: Toggle button opens/closes sidebar             │
│  • Desktop: Always visible (fixed position)               │
│  • Overlay: Click outside to close on mobile              │
└─────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────┐
│                    Header Component                      │
│                                                           │
│  No State (Stateless)                                     │
│  • Search input ready for state management                │
│  • Notification bell ready for integration                │
│  • Profile button ready for dropdown menu                 │
└─────────────────────────────────────────────────────────┘
```

## 🔄 Data Flow

```
Page Load
    │
    ├─→ Layout.tsx renders
    │       │
    │       ├─→ Sidebar.tsx (layout/Sidebar)
    │       │       └─→ Loads navigation items
    │       │
    │       └─→ Header.tsx (layout/Header)
    │               └─→ Renders search & profile
    │
    └─→ Page.tsx renders (children)
            │
            ├─→ Fetches dashboard data
            ├─→ Displays stats cards
            └─→ Shows recent activity
```

## 🎯 Responsive Breakpoints

```
┌─────────────────────────────────────────────────────────┐
│ Mobile (<1024px / lg)                                    │
├─────────────────────────────────────────────────────────┤
│ • Sidebar: Hidden by default, overlay when open          │
│ • Header: Compact, hide some labels                      │
│ • Stats: 1 column grid                                   │
│ • Toggle Button: Visible                                 │
└─────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────┐
│ Tablet (768px - 1024px / md to lg)                       │
├─────────────────────────────────────────────────────────┤
│ • Sidebar: Can be toggled                                │
│ • Header: Full width with all elements                   │
│ • Stats: 2 column grid                                   │
└─────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────┐
│ Desktop (≥1024px / lg+)                                  │
├─────────────────────────────────────────────────────────┤
│ • Sidebar: Fixed, always visible (256px width)           │
│ • Header: Full features visible                          │
│ • Stats: 4 column grid                                   │
│ • Main content: ml-64 (margin-left for sidebar)          │
└─────────────────────────────────────────────────────────┘
```

## 🚀 Performance Optimizations

- ✅ **Client Components**: Only interactive components marked with "use client"
- ✅ **Code Splitting**: Separate files for layout and UI components
- ✅ **Tree Shaking**: Export only what's needed via index files
- ✅ **CSS**: Tailwind CSS purges unused styles in production
- ✅ **Icons**: Lucide React uses tree-shakeable imports
- ✅ **Lazy Loading**: Ready for dynamic imports when needed

## 🔌 Extension Points

### Add Authentication
```typescript
// components/layout/Sidebar.tsx
// Add logout button in profile section

// components/layout/Header.tsx  
// Add user dropdown menu
```

### Add Loading States
```typescript
// Use the Loader components
import { FullPageLoader } from "@/components/ui";

// In any page
if (loading) return <FullPageLoader text="Loading..." />;
```

### Add More UI Components
```
src/components/ui/
  ├── badge.tsx        (Status badges)
  ├── modal.tsx        (Modals/Dialogs)
  ├── dropdown.tsx     (Dropdown menus)
  ├── table.tsx        (Data tables)
  └── index.ts         (Export all)
```
